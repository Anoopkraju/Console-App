namespace EmployeeIQuittable
{
    // Define a contract for classes that can perform a Quit action.
    public interface IQuittable
    {
        // Require an implementation that returns no value.
        void Quit();
    }
}
